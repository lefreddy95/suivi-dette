# === Convex (projet dedie a suivi-dette) ===
VITE_CONVEX_URL=https://different-opossum-825.eu-west-1.convex.cloud

# === Clerk (réutilise l'instance Clerk de lobry-sms-brocante) ===
# Clé publishable, safe à commit (mais mieux en .env local pour la prod).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# === Worker Pushbullet (relaye vers MacroDroid pour l'envoi de SMS) ===
# L'URL du worker déployé sur le VPS IONOS (admin.ableiges.com).
# Doit exposer un endpoint POST /send-sms avec body { to, body }.
# Côté serveur (variable d'env Convex, pas VITE_) :
#   PUSHBULLET_WORKER_URL=https://admin.ableiges.com
